# CRM interactive UI priority under host contention (cgroup v2).
# Backup before apply; do not starve PostgreSQL.

[Service]
CPUWeight=500
IOWeight=500
MemoryLow=512M
MemoryMin=256M
OOMScoreAdjust=-200
Nice=-5
